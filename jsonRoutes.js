//this should probably be modularized?
var require = ("express");

var app = express();

app.post("./viewTables", function (req, res) {

    if (tables < 5) {
        //allow the table to be posted
    }
    else {
        //
    }

});